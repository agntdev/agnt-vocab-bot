[
  {
    "name": "Add Deck",
    "steps": [
      {
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Build a private deck your way. Type cards one by one, or import a CSV with prompt,answer,example columns."
            }
          }
        ],
        "send": {
          "callback": "deck:create"
        }
      }
    ]
  }
]
